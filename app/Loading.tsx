import { Layout, LayoutTitle } from "@/src/components/Layout";


export default function Loading() {
    return (
        <Layout>
            <LayoutTitle className={'flex h-full items-center justify-center'}>
                ... Loading
            </LayoutTitle>
        </Layout>)

}