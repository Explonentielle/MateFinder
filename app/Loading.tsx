import { Layout, LayoutTitle } from "@/src/components/Layout";


export default function Loading() {
    return (
        <Layout>
            <LayoutTitle className={'flex h-[80vh] items-center justify-center'}>
                ... Loading
            </LayoutTitle>
        </Layout>)

}